import { JwtRefreshGuardGuard } from './jwt.refresh.guard.guard';

describe('JwtRefreshGuardGuard', () => {
  it('should be defined', () => {
    expect(new JwtRefreshGuardGuard()).toBeDefined();
  });
});
